spring:
  application:
    name: inventory
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
      format_sql: true
  datasource:
    url: ${ORCHESTRATION_INVENTORY_DB_URL}
    username: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}
    driver-class-name: org.postgresql.Driver
  rabbitmq:
    host: ${RABBITMQ_HOST}
    virtual-host: ${SPRING_RABBITMQ_VHOST}
    port: ${RABBITMQ_PORT}
    username: ${RABBITMQ_USERNAME}
    password: ${RABBITMQ_PASSWORD}
    listener:
      simple:
        observation-enabled: true
    orchestrator:
      reply:
        queue: ${ORCHESTRATOR_INVENTORY_REPLY_QUEUE}
        exchange: ${ORCHESTRATOR_INVENTORY_REPLY_EXCHANGE}
        routingKey: ${ORCHESTRATOR_INVENTORY_REPLY_ROUTING_KEY}
    inventory:
      command:
        queue: ${INVENTORY_COMMAND_QUEUE}
      fail:
        command:
          queue: ${INVENTORY_FAIL_COMMAND_QUEUE}

management:
  endpoints:
    web:
      exposure:
        include: "health,info,prometheus"
  prometheus:
    metrics:
      export:
        enabled: true
  metrics:
    tags:
      application: "orchestration-inventory"
    distribution:
      percentiles-histogram:
        http.server.requests: true
  tracing:
    propagation:
      type: W3C
    sampling:
      probability: 1.0
  otlp:
    metrics:
      export:
        enabled: false
  opentelemetry:
    tracing:
      export:
        otlp:
          endpoint: http://jaeger:4318/v1/traces
          timeout: 30s
        schedule-delay: 1s
        max-batch-size: 256
        max-queue-size: 2048

server:
  port: 8080

