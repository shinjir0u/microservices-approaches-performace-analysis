spring:
  application:
    name: payment
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
      format_sql: true
  datasource:
    url: ${PAYMENT_DB_URL}
    username: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}
    driver-class-name: org.postgresql.Driver
  rabbitmq:
    host: ${RABBITMQ_HOST}
    virtual-host: ${SPRING_RABBITMQ_VHOST}
    port: ${RABBITMQ_PORT}
    username: ${RABBITMQ_USERNAME}
    password: ${RABBITMQ_PASSWORD}
    order:
      created:
        payment:
          queue: ${ORDER_CREATED_PAYMENT_QUEUE}
    inventory:
      failed:
        payment:
          queue: ${INVENTORY_FAILED_PAYMENT_QUEUE}
    payment:
      charged:
        queue: ${PAYMENT_CHARGED_QUEUE}
        exchange: ${PAYMENT_CHARGED_EXCHANGE}
        routingKey: ${PAYMENT_CHARGED_ROUTINGKEY}
      failed:
        order:
          queue: ${PAYMENT_FAILED_ORDER_QUEUE}
        inventory:
          queue: ${PAYMENT_FAILED_INVENTORY_QUEUE}
        exchange: ${PAYMENT_FAILED_EXCHANGE}
        routingKey: ${PAYMENT_FAILED_ROUTINGKEY}

management:
  endpoints:
    web:
      exposure:
        include: "health,info,prometheus"
  endpoint:
    prometheus:
      enabled: true
  metrics:
    tags:
      application: "choreography-payment"
    distribution:
      percentiles-histogram:
        http.server.requests: true
  tracing:
    sampling:
      probability: 1.0
  opentelemetry:
    metrics:
      export:
        enabled: false
    tracing:
      export:
        otlp:
          endpoint: http://jaeger:4318/v1/traces
          enabled: true

logging:
  level:
    io.micrometer.tracing: DEBUG
    io.opentelemetry: DEBUG
    org.springframework.boot.actuate.autoconfigure.tracing: DEBUG

server:
  port: 8080